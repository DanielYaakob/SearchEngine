# SearchEngine
This project uses Java and MySQl to query a database of 5000 movies and return recommended movies based on user input.

to successfully run this project:

--ensure all .csv files are in the correct directory.
--ensure you are running a local sql server (the java file will write to the database)
--run the code and observe the output.
